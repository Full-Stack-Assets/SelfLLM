torch>=2.0.0
numpy>=1.24.0
tqdm>=4.65.0
pyyaml>=6.0
fastapi>=0.100.0
uvicorn>=0.23.0
httpx>=0.24.0
gradio>=4.0.0
pytest>=7.0.0

# Optional: vector retrieval (RAG / long-context). Install with
#   pip install selfllm[rag]
# or directly: pip install faiss-cpu
# faiss-cpu>=1.7.0
