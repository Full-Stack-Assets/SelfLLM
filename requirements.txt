# Lower bounds are kept at known-secure minimums so the declared ranges no
# longer admit versions with published advisories (scanners flag the floor, not
# the latest resolved version). torch is intentionally held at >=2.2.0 to stay
# compatible with the CI-pinned torch==2.2.2+cpu wheel; advancing it further
# requires bumping the CI pin in lockstep (see PR notes).
torch>=2.2.0
numpy>=1.26.0
tqdm>=4.66.3
pyyaml>=6.0.1
fastapi>=0.109.1
uvicorn>=0.30.1
httpx>=0.27.0
gradio>=4.44.1
jinja2>=3.1.6
pytest>=7.4.4
pytest-cov>=5.0.0

# Optional: vector retrieval (RAG / long-context). Install with
#   pip install selfllm[rag]
# or directly: pip install faiss-cpu
# faiss-cpu>=1.7.0
