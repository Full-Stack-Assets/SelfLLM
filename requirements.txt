# Lower bounds are kept at known-secure minimums so the declared ranges no
# longer admit versions with published advisories (scanners flag the floor, not
# the latest resolved version). The torch floor is >=2.6.0 to clear the
# torch.load RCE (CVE-2025-32434) and is kept in lockstep with the CI pin
# torch==2.12.1+cpu (see .github/workflows/*.yml).
torch>=2.6.0
numpy>=1.26.0
tqdm>=4.66.3
pyyaml>=6.0.1
fastapi>=0.109.1
uvicorn>=0.30.1
httpx>=0.27.0
gradio>=4.44.1
jinja2>=3.1.6
pytest>=7.4.4
pytest-cov>=5.0.0

# Optional: vector retrieval (RAG / long-context). Install with
#   pip install selfllm[rag]
# or directly: pip install faiss-cpu
# faiss-cpu>=1.7.0
