name: Performance Benchmark

on:
  pull_request:
    branches: [main]
    paths:
      # Only run when serving, model, or tokenizer code changes
      - "selfllm/serving/**"
      - "selfllm/model/**"
      - "run_training.py"
      - "run_self_improve.py"
  workflow_dispatch:
    inputs:
      max_new_tokens:
        description: "Max new tokens per generation (default: 32)"
        required: false
        default: "32"
      batch_size:
        description: "Batch size for throughput benchmark (default: 1)"
        required: false
        default: "1"

concurrency:
  group: perf-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ---------------------------------------------------------------------------
  # Serving engine benchmark (optimized.py vs baseline)
  # ---------------------------------------------------------------------------
  benchmark-serving:
    name: Serving Engine Benchmark
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install torch==2.2.2+cpu --index-url https://download.pytorch.org/whl/cpu
          pip install numpy==1.26.4
          pip install -e ".[dev]"

      - name: Run optimized.py tests
        run: pytest tests/test_optimized.py -v --tb=short

      - name: Run serving benchmark script
        run: |
          python scripts/benchmark.py \
            --max-new-tokens ${{ github.event.inputs.max_new_tokens || '32' }} \
            --batch-size ${{ github.event.inputs.batch_size || '1' }} \
            --timed-runs 3 \
            --output benchmark_results.json

      - name: Display results summary
        run: |
          python -c "
          import json, sys
          with open('benchmark_results.json') as f:
              data = json.load(f)
          print('=== Serving Benchmark Results ===')
          for row in data.get('results', []):
              print(f\"  {row['engine']:25s} | {row['workload']:20s} | \
          {row['tokens_per_sec']:7.1f} tok/s | avg={row['avg_latency_ms']:.0f}ms | \
          p95={row['p95_latency_ms']:.0f}ms\")
          speedups = [r for r in data.get('results', []) if r.get('speedup_vs_baseline')]
          if speedups:
              avg_speedup = sum(r['speedup_vs_baseline'] for r in speedups) / len(speedups)
              print(f'  Avg compiled speedup: {avg_speedup:.3f}x')
          "

      - name: Check for performance regression
        run: |
          python -c "
          import json, sys

          with open('benchmark_results.json') as f:
              data = json.load(f)

          BASELINE_TPS_FLOOR = 100.0   # tok/s — fail if baseline drops below this
          COMPILED_REGRESSION_THRESHOLD = 0.90  # compiled must be >= 90% of baseline

          failures = []
          baseline_tps = {}
          compiled_tps = {}

          for row in data.get('results', []):
              if row['engine'] == 'A_baseline_eager':
                  baseline_tps[row['workload']] = row['tokens_per_sec']
              elif row['engine'] == 'B_compiled':
                  compiled_tps[row['workload']] = row['tokens_per_sec']

          for wl, tps in baseline_tps.items():
              if tps < BASELINE_TPS_FLOOR:
                  failures.append(f'Baseline {wl}: {tps:.1f} tok/s < floor {BASELINE_TPS_FLOOR}')

          for wl, tps in compiled_tps.items():
              base = baseline_tps.get(wl)
              if base and tps < base * COMPILED_REGRESSION_THRESHOLD:
                  failures.append(
                      f'Compiled {wl}: {tps:.1f} tok/s is <90% of baseline {base:.1f}'
                  )

          if failures:
              print('PERFORMANCE REGRESSIONS DETECTED:')
              for f in failures:
                  print(f'  - {f}')
              sys.exit(1)
          else:
              print('No performance regressions detected.')
          "

      - name: Upload benchmark results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: benchmark-results-${{ github.sha }}
          path: benchmark_results.json
          retention-days: 30

  # ---------------------------------------------------------------------------
  # Tokenizer throughput benchmark
  # ---------------------------------------------------------------------------
  benchmark-tokenizer:
    name: Tokenizer Throughput
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install torch==2.2.2+cpu --index-url https://download.pytorch.org/whl/cpu
          pip install numpy==1.26.4
          pip install -e ".[dev]"

      - name: Run tokenizer tests
        run: pytest tests/test_tokenizer.py -v --tb=short

      - name: Tokenizer throughput check
        run: |
          python -c "
          import time, sys
          sys.path.insert(0, '.')
          from selfllm.model.tokenizer import BPETokenizer

          corpus = ['the quick brown fox jumps over the lazy dog'] * 50
          tok = BPETokenizer(vocab_size=512)
          tok.train(corpus)

          # Time 1000 encode calls
          text = ' '.join(corpus[:10])
          N = 200
          t0 = time.perf_counter()
          for _ in range(N):
              tok.encode(text)
          elapsed = time.perf_counter() - t0
          chars = len(text) * N
          tcs = chars / elapsed

          FLOOR = 500_000   # chars/sec — O(n log n) patch should give >1M
          print(f'Tokenizer throughput: {tcs:,.0f} chars/sec (floor={FLOOR:,})')
          if tcs < FLOOR:
              print(f'REGRESSION: tokenizer throughput {tcs:.0f} < {FLOOR}')
              sys.exit(1)
          else:
              print('Tokenizer throughput OK')
          "
